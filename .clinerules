# Meeplit 开发规范

## 项目结构规范

### Monorepo 结构
- 使用 Bun workspaces 管理三个包：`packages/server`、`packages/client`、`packages/shared`
- 共享类型和工具必须放在 `packages/shared` 中
- 包之间的依赖通过 workspace 引用，例如 `"@meeplit/shared": "workspace:*"`

### 代码组织
- **服务器代码**：`packages/server/src/`
  - `main.ts` - 应用入口点
  - `game/` - 游戏逻辑
  - `*.ts` - 业务逻辑模块
- **客户端代码**：`packages/client/src/`
  - `game/` - 游戏相关组件和服务
  - `views/` - 页面级组件
  - `components/` - 可复用组件
- **共享代码**：`packages/shared/`
  - `rpc.ts` - RPC 协议定义
  - `game.ts` - 游戏类型定义
  - `chat.ts` - 聊天类型定义

## 代码风格规范

### TypeScript
- 使用严格模式（`strict: true`）
- 优先使用接口（interface）而非类型别名（type），除非需要联合类型或映射类型
- 避免使用 `any`，使用 `unknown` 或具体类型
- 导出类型时使用 `export type { ... }` 语法
- 使用 `camelCase` 命名变量和函数，`PascalCase` 命名类和接口

### Vue 3 组件
- 使用 `<script setup>` 语法
- 组件文件名使用 `PascalCase`，例如 `GameInfo.vue`
- 组合式函数使用 `use` 前缀，例如 `useGameState`
- Props 使用 TypeScript 接口定义
- 避免在模板中使用复杂的逻辑，提取到计算属性或方法中

### RPC 通信
- RPC 方法命名使用 `camelCase`
- 服务器调用客户端方法时使用 `.` 分隔的路径，例如 `gameService.ask`
- 使用适当的调用模式：
  - `emit()` - 单向通知，不等待响应
  - `call()` - 请求-响应，需要返回值
  - `reserve()` - 收集请求用于批量执行
- 批量调用时指定执行模式：`sequential`（顺序）或 `parallel`（并行）

### 卡牌类设计
- 所有卡牌类必须继承自 `Card` 基类
- 必须使用 `@Revivable` 装饰器
- 必须实现 `img`、`name` 属性
- 可选实现 `description_url` 属性
- 可以在 `play()` 方法中实现卡牌效果

## 开发流程规范

### 分支策略
- `main` - 稳定版本
- `develop` - 开发分支
- `feature/*` - 功能分支
- `bugfix/*` - 修复分支

### 提交信息
- 使用 Conventional Commits 格式
- 类型前缀：`feat:`、`fix:`、`docs:`、`style:`、`refactor:`、`test:`、`chore:`
- 示例：`feat: 添加对手区组件显示玩家状态`

### 测试要求
- 核心 RPC 功能必须有单元测试
- 房间管理和玩家管理必须有集成测试
- 新增功能必须包含测试用例
- 运行测试：`bun test`

## 架构原则

### 反向 RPC 原则
- 游戏逻辑集中在服务器的一个 main 函数中
- 服务器主动询问客户端，而不是客户端请求服务器
- 客户端只负责展示和收集玩家输入
- 服务器驱动整个游戏流程

### 状态管理原则
- 服务器是唯一的状态源
- 客户端状态是服务器的投影
- 通过 RPC 同步状态变更
- 客户端本地状态仅用于 UI 交互

### 错误处理原则
- RPC 调用必须有超时处理
- 网络异常必须有重连机制
- 游戏状态异常必须有恢复机制
- 用户输入必须有验证和反馈

## 性能优化指南

### 网络优化
- 使用批量 RPC 调用减少网络往返
- 压缩传输数据
- 使用 WebSocket 保持连接
- 实现心跳机制检测连接状态

### 内存优化
- 及时清理不再使用的对象
- 使用 WeakMap/WeakSet 管理缓存
- 避免内存泄漏，特别是在事件监听器中
- 监控内存使用情况

### 渲染优化
- Vue 组件使用 `shallowRef` 优化性能
- 避免不必要的重新渲染
- 使用虚拟列表处理大量数据
- 图片懒加载和压缩

## 安全规范

### 输入验证
- 所有用户输入必须验证
- RPC 参数必须进行类型检查
- 防止 SQL 注入（使用参数化查询）
- 防止 XSS 攻击（转义用户内容）

### 认证授权
- 使用会话 ID 进行用户认证
- 房间操作必须验证玩家权限
- 敏感操作需要额外确认
- 记录操作日志

### 数据保护
- 密码必须哈希存储
- 敏感信息不记录在日志中
- 使用 HTTPS/WSS 传输
- 定期备份数据库

## 文档要求

### 代码注释
- 公共 API 必须有 JSDoc 注释
- 复杂算法必须有解释性注释
- TODO 注释必须包含负责人和预计完成时间
- FIXME 注释必须包含问题和解决方案

### 项目文档
- `README.md` - 项目概述和快速开始
- `memory-bank/` - 项目知识库
  - `projectbrief.md` - 项目概览
  - `activeContext.md` - 当前工作上下文
  - `progress.md` - 开发进度跟踪
- API 文档 - HTTP 和 RPC 接口文档

## 开发工作流规范

### 前端开发
- 前端代码使用 Vite 开发服务器运行
- 开发者自行查看样式效果，无需 AI 启动测试服务器
- AI 在修改前端代码后只需关注 TypeScript 报错
- 使用 `bun run dev` 启动开发服务器

### 代码质量检查
- 修改代码后运行 `bun run type-check` 检查 TypeScript 类型
- 使用 `bun run lint` 进行代码风格检查
- 提交前确保没有 TypeScript 错误

## 部署规范

### 环境配置
- 使用环境变量管理配置
- 不同环境使用不同配置文件
- 敏感信息不提交到版本控制
- 配置验证在启动时进行

### 构建部署
- 客户端使用 Vite 构建
- 服务器使用 Bun 运行
- 使用 Docker 容器化部署
- 实现健康检查端点

### 监控告警
- 实现 `/api/health` 健康检查
- 记录关键指标（在线玩家、房间数、错误率）
- 设置错误告警阈值
- 定期检查日志

---

*最后更新：2026-02-24*